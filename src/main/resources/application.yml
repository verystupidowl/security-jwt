spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/security_jwt
    username: postgres
    password: Ghbdtn12345
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false