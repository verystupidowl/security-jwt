version: "3.9"
name: "spring-jwt"
services:
  postgres:
    image: postgres:13.3
    environment:
      POSTGRES_DB: "spring-jwt"
      POSTGRES_USER: "root"
      POSTGRES_PASSWORD: "root"
    ports:
      - "5432:5432"